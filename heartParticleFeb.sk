every 10 seconds in "world":
    loop all players:
        if line 1 of lore of loop-player's held item contains "hearts":
            # need to add perms here
            make loop-player execute command "/particle heart ~ ~2 ~ 0.5 1 0.5 0 10"